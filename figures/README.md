# Reproducible figures

- `cost_components`: assumed energy, wear and time costs per kilometre.
- `impact_sensitivity`: impact ratios under different assumptions about additional travel.

Each figure is supplied as PNG and SVG. These are illustrative scenarios, not measured Maharashtra outcomes. Regenerate them with `python analysis/make_figures.py` from the repository root after installing the [build requirements](../requirements-build.txt).
